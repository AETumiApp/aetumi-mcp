{
  "_comment": "Cursor MCP config (~/.cursor/mcp.json or project .cursor/mcp.json). Same remote-URL shape works for Codex and other MCP-aware clients. Public server, no token required.",
  "mcpServers": {
    "aetumi": {
      "url": "https://mcp.aetumi.app"
    }
  }
}
